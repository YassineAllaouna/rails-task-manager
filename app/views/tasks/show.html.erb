<h2><%= @tasks.title %></h2>
<p><%= @tasks.details %></p>
<p><%= @tasks.completed %></p>
<p><%= link_to "Edit a Task", edit_task_path%> || <%= link_to "Delete",
            task_path(@tasks),
            method: :delete,
            data: { confirm: "Are you sure?" } %></p>
<p><%= link_to "Mes Tâches", tasks_path%> || </p>
